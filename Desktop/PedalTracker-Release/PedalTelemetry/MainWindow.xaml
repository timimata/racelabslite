<Window x:Class="PedalTelemetry.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PedalTelemetry"
        Title="Pedal Telemetry"
        Height="320" Width="540"
        MinHeight="220" MinWidth="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        Closing="Window_Closing">
    <Border Background="#FF1A1A1A" CornerRadius="4" BorderBrush="#FF333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>  <!-- Title bar -->
                <RowDefinition Height="*"/>   <!-- Chart + bars -->
                <RowDefinition Height="28"/>  <!-- Status bar -->
                <RowDefinition Height="26"/>  <!-- Legend -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#FF222222" CornerRadius="4,4,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                        <Ellipse Width="8" Height="8" Fill="#FF00C800" x:Name="TitleDot" Margin="0,0,8,0"/>
                        <TextBlock Text="PEDAL TELEMETRY" Foreground="#FFCCCCCC" FontSize="11" FontWeight="SemiBold" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                        <Button Content="&#xE949;" Style="{StaticResource TitleButton}" Click="Settings_Click" FontFamily="Segoe MDL2 Assets" ToolTip="Settings"/>
                        <Button Content="&#xE921;" Style="{StaticResource TitleButton}" Click="Minimize_Click" FontFamily="Segoe MDL2 Assets"/>
                        <Button Content="&#xE8BB;" Style="{StaticResource TitleButton}" Click="Close_Click" FontFamily="Segoe MDL2 Assets"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Chart + Vertical Bars -->
            <Grid Grid.Row="1" Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>    <!-- Chart -->
                    <ColumnDefinition Width="60"/>   <!-- Bars -->
                </Grid.ColumnDefinitions>

                <!-- Telemetry Chart -->
                <local:TelemetryChart x:Name="Chart" Grid.Column="0" Margin="2"/>

                <!-- Vertical Bar Gauges -->
                <Grid Grid.Column="1" Margin="4,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Throttle Bar -->
                    <Grid Grid.Column="0" Margin="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Background="#FF111111" CornerRadius="2">
                            <Grid>
                                <!-- Fill from bottom -->
                                <Border Background="#FF00C800" CornerRadius="0,0,2,2" VerticalAlignment="Bottom" x:Name="ThrottleBar"/>
                                <!-- Percentage label -->
                                <TextBlock x:Name="ThrottlePercent" Text="0%" Foreground="White" FontSize="10" FontWeight="Bold" FontFamily="Segoe UI"
                                           HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,4,0,0"/>
                            </Grid>
                        </Border>
                        <TextBlock Text="THR" Foreground="#FF00C800" FontSize="9" FontWeight="Bold" FontFamily="Segoe UI"
                                   HorizontalAlignment="Center" Grid.Row="1" Margin="0,2,0,0"/>
                    </Grid>

                    <!-- Brake Bar -->
                    <Grid Grid.Column="1" Margin="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Background="#FF111111" CornerRadius="2">
                            <Grid>
                                <Border Background="#FFE02020" CornerRadius="0,0,2,2" VerticalAlignment="Bottom" x:Name="BrakeBar"/>
                                <TextBlock x:Name="BrakePercent" Text="0%" Foreground="White" FontSize="10" FontWeight="Bold" FontFamily="Segoe UI"
                                           HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,4,0,0"/>
                            </Grid>
                        </Border>
                        <TextBlock Text="BRK" Foreground="#FFE02020" FontSize="9" FontWeight="Bold" FontFamily="Segoe UI"
                                   HorizontalAlignment="Center" Grid.Row="1" Margin="0,2,0,0"/>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="#FF222222" Padding="8,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Connection Status -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="7" Height="7" x:Name="StatusDot" Fill="#FFE02020" Margin="0,0,6,0"/>
                        <TextBlock x:Name="StatusLabel" Text="iRACING" Foreground="#FFCCCCCC" FontSize="10" FontWeight="Bold" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" Text="  Disconnected" Foreground="#FF888888" FontSize="10" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Delta -->
                    <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="12,0" HorizontalAlignment="Center">
                        <TextBlock Text="DELTA" Foreground="#FF666666" FontSize="8" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="DeltaLabel" Text="—" Foreground="#FF888888" FontSize="11" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Last Lap -->
                    <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Center" Margin="12,0" HorizontalAlignment="Center">
                        <TextBlock Text="LAST LAP" Foreground="#FF666666" FontSize="8" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="LastLapLabel" Text="—" Foreground="#FF888888" FontSize="11" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Best -->
                    <StackPanel Grid.Column="4" Orientation="Vertical" VerticalAlignment="Center" Margin="12,0,0,0" HorizontalAlignment="Center">
                        <TextBlock Text="BEST" Foreground="#FF666666" FontSize="8" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="BestLapLabel" Text="—" Foreground="#FF888888" FontSize="11" FontWeight="Bold" FontFamily="Segoe UI" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Legend / Controls -->
            <Border Grid.Row="3" Background="#FF1A1A1A" Padding="8,0" CornerRadius="0,0,4,4">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="7" Height="7" Fill="#FF00C800" Margin="0,0,4,0"/>
                        <TextBlock Text="Throttle" Foreground="#FF888888" FontSize="9" FontFamily="Segoe UI" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Ellipse Width="7" Height="7" Fill="#FFE02020" Margin="0,0,4,0"/>
                        <TextBlock Text="Brake" Foreground="#FF888888" FontSize="9" FontFamily="Segoe UI" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="iRacing" Foreground="#FF4488FF" FontSize="9" FontWeight="Bold" FontFamily="Segoe UI" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Opacity" Foreground="#FF666666" FontSize="9" FontFamily="Segoe UI" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <Slider x:Name="OpacitySlider" Width="80" Minimum="0.3" Maximum="1.0" Value="1.0" VerticalAlignment="Center"
                                ValueChanged="OpacitySlider_ValueChanged"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
